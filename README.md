alright, first readme file, of course its not going to help

run this command to get started:

pip install -r requirements.txt


then run:

python manage.py runserver

* Make sure you are on the main directory *